interface MainRentalProp {
  rentalCount: number;
}

interface OfferCardType {
  price: number;
  image: string;
  name: string;
  type: string;
  status: string;
}

export type {MainRentalProp, OfferCardType};
